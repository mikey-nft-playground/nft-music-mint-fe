import styled from '@emotion/styled'

export const UserIndicatorStyle = styled('div')(({ theme }: any) => {
  return {
    '.user-indicator': {
      display: 'flex',
      alignItems: 'center',
      marginLeft: '1rem'
    }
  }
})
